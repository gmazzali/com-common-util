<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jee="http://www.springframework.org/schema/jee"
	xmlns:lang="http://www.springframework.org/schema/lang" xmlns:util="http://www.springframework.org/schema/util"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/lang http://www.springframework.org/schema/lang/spring-lang-3.1.xsd
                http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.1.xsd
       			http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
                http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
                http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.1.xsd
                http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">

	<!-- ========================================================================================== -->
	<!-- ==================== CARGAMOS LOS ARCHIVOS DE PROPIEDADES DEL SISTEMA ==================== -->
	<!-- ========================================================================================== -->

	<bean id="properties" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
			<list>
				<!-- EL LISTADO DE LAS UBICACIONES DE LOS ARCHIVOS DE CONFIGURACIÓN -->
				<value>file:///${proyecto.configuration.dir}/jdbc.properties</value>
				<value>file:///${proyecto.configuration.dir}/configuration.properties</value>
			</list>
		</property>
	</bean>

	<!-- ========================================================================================= -->
	<!-- =============== CREAMOS LA LOCALIDAD DONDE VAMOS A EJECUTAR LA APLICACIÓN =============== -->
	<!-- ========================================================================================= -->

	<bean id="messageLocale" class="java.util.Locale" lazy-init="false" depends-on="properties">
		<constructor-arg index="0" value="${message.locale.languaje}" />
		<constructor-arg index="1" value="${message.locale.country}" />
	</bean>

	<!-- =========================================================================================== -->
	<!-- ================ CARGAMOS LOS MENSAJES QUE VAMOS A USAR DENTRO DEL SISTEMA ================ -->
	<!-- =========================================================================================== -->

	<bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
		<property name="cacheSeconds" value="600" />
		<property name="basenames">
			<list>
				<!-- LA UBICACIÓN DONDE TENEMOS LOS ARCHIVOS DE LOS MENSAJES -->
				<value>file:///${proyecto.configuration.dir}/messages/messages</value>
			</list>
		</property>
	</bean>

	<!-- Si los métodos de seteo son estático -->

	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="messageSource">
    	<property name="staticMethod" value="com.common.util.holder.HolderMessage.setResources"/>
    	<property name="arguments">
        	<list>
            	<ref bean="messageSource"/>
        	</list>
   		</property>
	</bean>
	
	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="messageLocale">
    	<property name="staticMethod" value="com.common.util.holder.HolderMessage.setLocale"/>
    	<property name="arguments">
        	<list>
            	<ref bean="messageLocale"/>
        	</list>
   		</property>
	</bean>

	<!-- O si los métodos de seteo no son estático -->

	<bean id="holderMessage" class="com.common.util.holder.HolderMessage" lazy-init="false" depends-on="messageSource, messageLocale">
		<property name="resources" ref="messageSource" />
		<property name="locale" ref="messageLocale" />
	</bean>

	<!-- ======================================================================================= -->
	<!-- ==================== CARGAMOS LA CONFIGURACIÓN PARA EL FORMATEADOR ==================== -->
	<!-- ======================================================================================= -->

	<bean id="formatterLocale" class="java.util.Locale" lazy-init="false" depends-on="properties">
		<constructor-arg index="0" value="${formatter.locale.languaje}" />
		<constructor-arg index="1" value="${formatter.locale.country}" />
	</bean>
	
	<!-- Si los métodos de seteo son estático -->

	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="formatterLocale">
    	<property name="staticMethod" value="com.common.util.tools.FormatUtil.setDateFormat"/>
    	<property name="arguments">
        	<list>
            	<ref bean="messageSource"/>
        	</list>
   		</property>
	</bean>
	
	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="properties">
    	<property name="staticMethod" value="com.common.util.tools.FormatUtil.setLocale"/>
    	<property name="arguments">
        	<list>
        		<value>${formatter.pattern.date}</value>
        	</list>
   		</property>
	</bean>

	<!-- O si los métodos de seteo no son estático -->

	<bean id="formatter" class="com.common.util.tools.FormatUtil" lazy-init="false" depends-on="properties, formatterLocale">
		<property name="locale" ref="formatterLocale" />
		<property name="dateFormat" ref="${formatter.pattern.date}" />
	</bean>

	<!-- ======================================================================================= -->
	<!-- ================ CARGAMOS LA CONFIGURACIÓN PARA EL CONVERSOR DE MONEDA ================ -->
	<!-- ======================================================================================= -->
	
	<!-- Si los métodos de seteo son estático -->

	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="properties">
    	<property name="staticMethod" value="com.common.util.tools.CurrencyUtil.setCurrencySimbol"/>
    	<property name="arguments">
        	<list>
        		<value>${currency.simbol}</value>
        	</list>
   		</property>
	</bean>
	
	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="properties">
    	<property name="staticMethod" value="com.common.util.tools.CurrencyUtil.setDecimals"/>
    	<property name="arguments">
        	<list>
        		<value>${currency.decimals}</value>
        	</list>
   		</property>
	</bean>
	
	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="properties">
    	<property name="staticMethod" value="com.common.util.tools.CurrencyUtil.setGroupSeparator"/>
    	<property name="arguments">
        	<list>
        		<value>${currency.separator.group}</value>
        	</list>
   		</property>
	</bean>
	
	<bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false" depends-on="properties">
    	<property name="staticMethod" value="com.common.util.tools.CurrencyUtil.setDecimalSeparator"/>
    	<property name="arguments">
        	<list>
        		<value>${currency.separator.decimal}</value>
        	</list>
   		</property>
	</bean>
	
	<!-- O si los métodos de seteo no son estático -->
	
	<bean id="currencyUtil" class="com.common.util.tools.CurrencyUtil" lazy-init="false" depends-on="properties">
		<property name="currencySimbol" ref="${currency.simbol}" />
		<property name="decimals" ref="${currency.decimals}" />
		<property name="groupSeparator" ref="${currency.separator.group}" />
		<property name="decimalSeparator" ref="${currency.separator.decimal}" />
	</bean>

	<!-- ======================================================================================== -->
	<!-- =========== CARGAMOS LA CONFIGURACIÓN PARA EL CONVERSOR DE NUMEROS A STRINGS =========== -->
	<!-- ======================================================================================== -->

	<bean id="converterUtil" class="com.common.util.tools.ConverterUtil" lazy-init="false" depends-on="messageSource">
	<property name=""></property>
		<property name="tenConnector" value="${number.word.ten.connector}" />
		<property name="unitsGroup">
			<list>
				<value>${number.word.unit.0}</value>
				<value>${number.word.unit.1}</value>
				<value>${number.word.unit.2}</value>
				<value>${number.word.unit.3}</value>
				<value>${number.word.unit.4}</value>
				<value>${number.word.unit.5}</value>
				<value>${number.word.unit.6}</value>
				<value>${number.word.unit.7}</value>
				<value>${number.word.unit.8}</value>
				<value>${number.word.unit.9}</value>
				<value>${number.word.unit.10}</value>
				<value>${number.word.unit.11}</value>
				<value>${number.word.unit.12}</value>
				<value>${number.word.unit.13}</value>
				<value>${number.word.unit.14}</value>
				<value>${number.word.unit.15}</value>
				<value>${number.word.unit.16}</value>
				<value>${number.word.unit.17}</value>
				<value>${number.word.unit.18}</value>
				<value>${number.word.unit.19}</value>
				<value>${number.word.unit.20}</value>
				<value>${number.word.unit.21}</value>
				<value>${number.word.unit.22}</value>
				<value>${number.word.unit.23}</value>
				<value>${number.word.unit.24}</value>
				<value>${number.word.unit.25}</value>
				<value>${number.word.unit.26}</value>
				<value>${number.word.unit.27}</value>
				<value>${number.word.unit.28}</value>
				<value>${number.word.unit.29}</value>
			</list>
		</property>
		<property name="tensGroup">
			<list>
				<value>${number.word.ten.0}</value>
				<value>${number.word.ten.10}</value>
				<value>${number.word.ten.20}</value>
				<value>${number.word.ten.30}</value>
				<value>${number.word.ten.40}</value>
				<value>${number.word.ten.50}</value>
				<value>${number.word.ten.60}</value>
				<value>${number.word.ten.70}</value>
				<value>${number.word.ten.80}</value>
				<value>${number.word.ten.90}</value>
			</list>
		</property>
		<property name="hundredsGroup">
			<list>
				<value>${number.word.hundred.100s}</value>
				<value>${number.word.hundred.100}</value>
				<value>${number.word.hundred.200}</value>
				<value>${number.word.hundred.300}</value>
				<value>${number.word.hundred.400}</value>
				<value>${number.word.hundred.500}</value>
				<value>${number.word.hundred.600}</value>
				<value>${number.word.hundred.700}</value>
				<value>${number.word.hundred.800}</value>
				<value>${number.word.hundred.900}</value>
			</list>
		</property>
		<property name="millonsGroup">
			<list>
				<list>
					<value>${number.word.group.singular.1000}</value>
					<value>${number.word.group.plural.1000}</value>
				</list>
				<list>
					<value>${number.word.group.singular.1000000}</value>
					<value>${number.word.group.plural.1000000}</value>
				</list>
				<list>
					<value>${number.word.group.singular.1000000000000}</value>
					<value>${number.word.group.plural.1000000000000}</value>
				</list>
				<list>
					<value>${number.word.group.singular.1000000000000000000}</value>
					<value>${number.word.group.plural.1000000000000000000}</value>
				</list>
				<list>
					<value>${number.word.group.singular.1000000000000000000000000}</value>
					<value>${number.word.group.plural.1000000000000000000000000}</value>
				</list>
				<list>
					<value>${number.word.group.singular.1000000000000000000000000000000}</value>
					<value>${number.word.group.plural.1000000000000000000000000000000}</value>
				</list>
			</list>
		</property>
	</bean>

	<!-- ====================================================================================== -->
	<!-- =================== CARGAMOS LA CONFIGURACIÓN PARA EL LOG DE LOG4J =================== -->
	<!-- ====================================================================================== -->

	<bean name="log4j" class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
		<property name="targetClass" value="org.springframework.util.Log4jConfigurer" />
		<property name="targetMethod" value="initLogging" />
		<property name="arguments">
			<list>
				<!-- LA UBICACIÓN DE DONDE TENEMOS EL ARCHIVO DE CONFIGURACIÓN PARA EL LOG DE LOG4J -->
				<value>file:///${proyecto.configuration.dir}/log4j.properties</value>
			</list>
		</property>
	</bean>

	<!-- ======================================================================================== -->
	<!-- ===================== CARGAMOS LA CONFIGURACIÓN DE LOS CONVERSORES ===================== -->
	<!-- ======================================================================================== -->

	<bean id="conversionService" class="com.common.util.converter.impl.ConverterServiceImpl">
		<property name="converters">
			<list>
				<!-- EL LISTADO DE LAS CLASES QUE IMPLEMENTAN LA INTERFAZ GENERICCONVERTER -->
				<bean class="nombre.de.la.clase.que.es.una.implementación.de.la.interfaz.GenericCoverter" />
			</list>
		</property>
	</bean>

	<!-- ========================================================================================= -->
	<!-- ================ CARGAMOS LOS ELEMENTOS QUE ANOTAMOS DENTRO DEL CONTEXTO ================ -->
	<!-- ========================================================================================= -->

	<context:annotation-config />
	<context:component-scan base-package="com.*" />

	<!-- ========================================================================================= -->
	<!-- ==================== CARGAMOS LA CONFIGURACIÓN PARA LA BASE DE DATOS ==================== -->
	<!-- ========================================================================================= -->

	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource" depends-on="properties">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
	</bean>

	<bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean" depends-on="dataSource">
		<property name="dataSource" ref="dataSource" />
		<property name="packagesToScan" value="com.proyecto.*" />
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">${jdbc.dialect}</prop>
				<prop key="hibernate.show_sql">${jdbc.show_sql}</prop>
				<prop key="hibernate.format_sql">${jdbc.format_sql}</prop>

				<prop key="hibernate.connection.pool_size">${jdbc.pool_size}</prop>
				<prop key="hibernate.bytecode.use_reflection_optimizer">${jdbc.use_reflection_optimizer}</prop>
				<prop key="hibernate.hbm2ddl.auto">${jdbc.hbm_to_ddl}</prop>
			</props>
		</property>
	</bean>

	<!-- ===================================================================================== -->
	<!-- ============ CREAMOS EL PROXY PARA LAS TRANSACCIONES A NIVEL DE SERVICIO ============ -->
	<!-- ===================================================================================== -->

	<bean id="transactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<aop:aspectj-autoproxy />
	<tx:annotation-driven transaction-manager="transactionManager" />

	<tx:advice id="txAdvice" transaction-manager="transactionManager">
		<tx:attributes>
			<tx:method name="save*" propagation="REQUIRED" read-only="false" />
			<tx:method name="update*" propagation="REQUIRED" read-only="false" />
			<tx:method name="remove*" propagation="REQUIRED" read-only="false" />
			<tx:method name="delete*" propagation="REQUIRED" read-only="false" />
			<tx:method name="execute*" propagation="REQUIRED" read-only="false" />
			<tx:method name="*" propagation="REQUIRED" read-only="true" />
		</tx:attributes>
	</tx:advice>

	<aop:config>
		<aop:pointcut id="baseService" expression="execution(* com.commons.util.service.*Service.*(..))" />
		<aop:pointcut id="servicePointCut" expression="execution(* ..service.*Service.*(..))" />

		<aop:advisor advice-ref="txAdvice" pointcut-ref="baseService" />
		<aop:advisor advice-ref="txAdvice" pointcut-ref="servicePointCut" />
	</aop:config>

</beans>